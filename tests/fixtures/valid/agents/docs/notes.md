# Agent prose, not an agent definition

This lives under `agents/docs/` and is reference prose, not an agent. Its parent
directory is `docs`, not `agents`, so the lint never treats it as an agent —
proving the `agents/docs/` exclusion.
