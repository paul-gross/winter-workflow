# Not an agent

This README.md sits inside an `agents/` directory beside a valid agent. The
lint must skip it — were it linted it would fail on every required key, so its
clean pass proves the README.md exclusion works.
