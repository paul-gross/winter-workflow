# Winter workflow

- **ADM review for agent-facing markdown** — after creating or modifying any markdown file that AI agents consume (agents, skills, CLAUDE.md, `ai/`), spawn the `agentic-development-manager` agent to review before committing.
