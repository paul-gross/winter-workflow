---
description: "Score the current codebase through the reusable harness-maturity process. Use weekly to track progress or divergence."
allowed-tools: Bash, Read, Glob, Grep, Write, Agent
---

# Harness Score

`harness-score` binds the reusable scoring process to the current working directory: it accepts no arguments, and its
target is `$PWD`.

## Execute

Read `winter-workflow:/methodology/harness-score/process.md` and execute it against `$PWD`.
